import type { DashboardUser } from "./types";

/** Fixture identity used by the design-preview routes only. */
export const previewUser: DashboardUser = {
  name: "محمد عثمان أحمد",
  role: "المواطن",
};
